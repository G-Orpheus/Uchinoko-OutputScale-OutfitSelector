# 出力倍率調整機能

「こだわり設定」に `出力倍率` を追加した改変版です。

- 既定値: `1.00`（元の挙動と同じ）
- 設定範囲: `0.01` ～ `100.00`
- 例: `0.50` で半分、`2.00` で2倍
- 倍率はアバターごとの `job.json` に `output_scale` として保存・復元されます。
- 倍率変更後は「フル変換(MOD作成)」を実行してください。「影のみ更新」では反映されません。

UE経由ではFBX出力時にメッシュと骨格全体へ同じ倍率を適用します。通常のUE不要経路では、頂点座標とRefSkeleton用のボーン位置へ同じ倍率を適用します。
